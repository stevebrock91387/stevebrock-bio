# Copy to functions/.env and fill in. functions/.env is gitignored — never commit the key.
# Reuse the EXISTING Resend key (the one the Decoupage feedback function already uses);
# do not create a new one.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
# Where the function 303-redirects the visitor after submit.
SITE_URL=https://stevebrock.bio
